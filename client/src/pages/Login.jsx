import React from "react";
import Form from "../components/Form";

const Login = () => {
    return (
        <div>
            <h1>Login</h1>
            <Form type="login" />
        </div>
    );
};

export default Login;
